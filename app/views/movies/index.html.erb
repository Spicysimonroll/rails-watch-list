<h1>Movies</h1>
<ul>
  <% @movies.each do |movie| %>
    <li>
      <h2><%= movie.title %></h2>
      <p><%= movie.rating %></p>
      <p>Overview: <%= movie.overview %></p>
      <img src="<%= movie.poster_url %>" alt=""></p>
    </li>
  <% end %>
</ul>
