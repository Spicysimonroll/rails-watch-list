<%= render 'navbar' %>
<h1 class="mt-5 pt-5 text-center">New movie list</h1>
<div class="container">
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name, label: "Movie list name", error: 'List name is mandatory, please specify one' %>
    <%= f.button :submit, "Add list", class: "btn-primary" %>
  <% end %>
</div>
